<!DOCTYPE html>
<html>
<head>
	<title>Edit Song</title>
</head>
<body>
	<h1>Edit Song</h1>
	<form method='post' action="/songs/<%= @song.slug%>">
		<input type="hidden" name="_method" id="hidden" value="patch">
		<p>Name: <input type="text" name="Name" value="<%= @song.name%>"></p>
		<p>Artist: <input type="text" name="Artist Name" value="<%= @song.artist.name%>"></p>
		<p>Genres: </p>
		<% @genres.each do |genre| %>
			<input type="checkbox" name="genres[]" id="<%= genre.name %>" value="<%= genre.name %>" <%= 'checked' if @song.genres.find{|g| g.name == genre.name}%>><%= genre.name %></input>
		<% end %>
		<p><input type="submit" id="submit" value="Save"></p>
	</form>

</body>
</html>