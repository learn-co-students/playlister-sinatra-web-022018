<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form class="" action="/songs/<%=@song.slug%>" method="post">
      <input type="hidden" name="_method" value="patch">
      <input type="text" name="song[name]" value="<%=@song.name%>" id="Name">
      <% Genre.all.each do |genre| %>
        <input type="checkbox" name="song[genre_ids][]" value="<%=genre.id%>" id="<%=genre.name%>" <%="checked" if @song.genre_ids.include?(genre.id)%>>
          <%=genre.name%>
        </input>
      <% end %>

      <input type="text" name="song[artist]" value="<%=@song.artist.name%>" id="Artist Name">

      <input type="submit" name="submit" value="Save">
    </form>
  </body>
</html>
