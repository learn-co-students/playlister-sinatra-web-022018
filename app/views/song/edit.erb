<html>
<head></head>
<body>
<form action='/songs/<%=@song.slug%>' method="post">
  <input type="hidden" name="_method" value="patch">
  Song name:<input type="text" name="song[name]" id="Name" value="<%= @song.name%>"><br>
  Artist:<input type="text" name="song[artist]" id="Artist Name" value="<%= @song.artist.name%>"><br>
  Genres:<% @genres.each do |genre| %><br>
  <input type="checkbox" id="<%=genre.name%>" name="song[genre_ids][]" value="<%=genre.id%>" <%if @song.genres.include?(genre)%>checked<%end%><%=genre.name %><br>
  <% end%>
  <input type="submit" value="Save">
</form>
</body>
</html>
